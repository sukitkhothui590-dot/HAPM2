# 🚀 วิธีตั้งค่าระบบอีเมลที่ใช้ง่ายที่สุด (ไม่ต้องใช้โดเมน)

## 🎯 สำหรับการทดสอบและเพิ่มผู้ใช้ใหม่

---

## ✅ วิธีที่แนะนำ: Resend + Verify Email (ง่ายที่สุด)

### 📋 ขั้นตอนการตั้งค่า (5 นาที):

#### 1. สมัคร Resend (ฟรี)
- ไปที่ https://resend.com
- คลิก **"Sign Up"** (ใช้ GitHub/Google ได้)
- Verify email ของคุณ

#### 2. สร้าง API Key
- ไปที่ https://resend.com/api-keys
- คลิก **"Create API Key"**
- ใส่ชื่อ: `Supabase Email System`
- คลิก **"Add"**
- **คัดลอก API Key** ที่ได้ (เริ่มต้นด้วย `re_`)

#### 3. ตั้งค่าใน Supabase Secrets
- ไปที่ Supabase Dashboard: https://supabase.com/dashboard
- เลือก Project ของคุณ
- ไปที่ **Project Settings** > **Edge Functions** > **Secrets**
- คลิก **"New secret"**
- ใส่ข้อมูล:
  ```
  Name: RESEND_API_KEY
  Value: re_xxxxx (API Key ที่คัดลอกมา)
  ```
- คลิก **"Create secret"**

#### 4. Verify Email ที่ต้องการส่งไป
- ไปที่ https://resend.com/emails
- คลิก **"Verify Email"** หรือ **"Add Email"**
- ใส่ email ที่ต้องการส่งไป (เช่น `test@gmail.com`, `user@example.com`)
- Resend จะส่ง verification email ไป
- **เปิดอีเมลและคลิกลิงค์เพื่อ verify**
- ✅ Email จะถูก verify และพร้อมใช้งาน

#### 5. ทดสอบ
- ไปที่ Admin > Users > เพิ่มผู้ใช้ใหม่
- ใส่ email ที่ verify แล้ว
- คลิก **"สร้างผู้ใช้"**
- ✅ ควรส่งอีเมลได้สำเร็จ!

---

## 💡 Tips สำหรับการใช้งาน

### สำหรับการทดสอบ:
1. **Verify email ของตัวเองก่อน** (เช่น `your-email@gmail.com`)
2. **ทดสอบส่งอีเมล** ไปที่ email ที่ verify แล้ว
3. **ตรวจสอบ Inbox และ Spam folder**

### สำหรับเพิ่มผู้ใช้ใหม่:
1. **เมื่อมี user ใหม่** → ต้อง verify email ของ user นั้นก่อน
2. **วิธี verify:**
   - ไปที่ https://resend.com/emails
   - คลิก **"Verify Email"**
   - ใส่ email ของ user
   - User จะได้รับ verification email
   - User คลิกลิงค์เพื่อ verify
   - ✅ พร้อมใช้งาน

### ถ้ามี user เยอะ:
- **แนะนำ:** Verify domain แทน (ส่งได้ทุก email โดยไม่ต้อง verify)
- ดูคู่มือ: `docs/EMAIL_SETUP.md`

---

## ⚠️ ข้อจำกัด

- **ต้อง verify recipient email** ทีละตัว
- ถ้ามี user ใหม่ ต้อง verify email ของ user นั้นก่อน
- **ไม่เหมาะสำหรับ production** ที่มี user เยอะ

---

## 🔄 ทางเลือกอื่น

### ถ้าต้องการส่งได้ทุก email โดยไม่ต้อง verify:

#### ตัวเลือก 1: Verify Domain ใน Resend (แนะนำ)
- ตั้งค่า DNS records ครั้งเดียว (5-30 นาที)
- ส่งได้ทุก email โดยไม่ต้อง verify
- เหมาะสำหรับ production
- ดูคู่มือ: `docs/EMAIL_SETUP.md`

#### ตัวเลือก 2: ใช้ Email Service อื่น
- **SendGrid** - ฟรี 100 อีเมล/วัน
- **Mailgun** - ฟรี 5,000 อีเมล/เดือน
- **AWS SES** - ราคาถูก

---

## 📝 Checklist

- [ ] สมัคร Resend account
- [ ] สร้าง API Key
- [ ] ตั้งค่า `RESEND_API_KEY` ใน Supabase Secrets
- [ ] Verify email ที่ต้องการส่งไป
- [ ] ทดสอบส่งอีเมล
- [ ] ตรวจสอบ Inbox และ Spam folder

---

## 🆘 ถ้ายังไม่ทำงาน

### 1. ตรวจสอบ Logs
- Supabase Dashboard > Edge Functions > Logs
- ดู error messages

### 2. ตรวจสอบ Secrets
- ตรวจสอบว่า `RESEND_API_KEY` ถูกตั้งค่าถูกต้อง
- ตรวจสอบว่า API Key ถูกต้อง (เริ่มต้นด้วย `re_`)

### 3. ตรวจสอบ Email Verification
- ไปที่ https://resend.com/emails
- ตรวจสอบว่า email ที่ต้องการส่งไปถูก verify แล้ว

### 4. ทดสอบ API โดยตรง
```bash
curl -X POST https://YOUR_PROJECT.supabase.co/functions/v1/send-email \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "verified-email@example.com",
    "subject": "Test Email",
    "html": "<h1>Test</h1>"
  }'
```

---

## 📞 สรุป

**สำหรับการทดสอบและเพิ่มผู้ใช้ใหม่:**
- ✅ **Resend + Verify Email** - ง่ายที่สุด ไม่ต้องเปลี่ยน code
- ✅ ตั้งค่าแค่ 5 นาที
- ✅ ใช้งานได้ทันที

**สำหรับ Production:**
- ✅ **Resend + Domain Verification** - แนะนำที่สุด
- ✅ ส่งได้ทุก email โดยไม่ต้อง verify
- ✅ ตั้งค่า DNS ครั้งเดียว

